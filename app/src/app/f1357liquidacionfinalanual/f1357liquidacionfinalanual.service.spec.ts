import { TestBed } from '@angular/core/testing';

import { F1357liquidacionfinalanualService } from './f1357liquidacionfinalanual.service';

xdescribe('F1357liquidacionfinalanualService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: F1357liquidacionfinalanualService = TestBed.get(F1357liquidacionfinalanualService);
    expect(service).toBeTruthy();
  });
});
